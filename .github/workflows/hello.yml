name: Hello Actions

on:
  push:            # 任何 push 會觸發
  workflow_dispatch:  # 也可手動觸發

jobs:
  hello:
    runs-on: ubuntu-latest  # 跑在 GitHub 提供的 Ubuntu 機器
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # 把你的程式碼拉到 runner 上

      - name: Say hello
        run: echo "Hello from GitHub Actions!"
